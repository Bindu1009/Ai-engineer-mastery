# AI Engineer Mastery - 10 Week Journey
 Day 1 (April 23, 2026): Banking System with Python OOP

**What I learned:**
- Classes and Object-Oriented Programming
- Private attributes (encapsulation with `_balance`)
- Properties (`@property` decorator)
- Custom exceptions (`InsufficientFundsError`, `InvalidAmountError`)
- Type hints (`-> float`, `List[Transaction]`)
- Logging with Python's `logging` module
- Git workflow (init, add, commit, push)

**What I built:**
- Complete banking system with deposit, withdraw, and transfer functionality
- Transaction history tracking
- Proper error handling for invalid operations
- Comprehensive test suite

**Code location:** `/day-1-banking-system/banking_system.py`

**Key insights:**
- Private attributes are convention-based in Python (not enforced)
- Properties allow controlled access to internal state
- Custom exceptions make error handling more expressive
- Logging is better than print() for production code

**Next up (Day 2):** Decorators and advanced Python features
